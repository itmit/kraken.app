namespace kraken
{
    public static class Constants
    {
        // URL of REST service
		public const string RestUrl = "http://accproject.beget.tech/api/{0}";
		public const string StorageUrl = "http://accproject.beget.tech{0}";
        // Credentials that are hard coded into the REST service
        public const string Username = "Xamarin";
        public const string Password = "pass";
    }
}
